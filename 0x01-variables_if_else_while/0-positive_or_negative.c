#include <stdio.h>

/**
 * main - Positive or Negative
 * Return: Return 0
 */

int main(void)
{
	int n = 5;

		if (n > 0)
		{
			printf("%d is positive\n");
		} else if
			(n == 0);
		{
			printf("%d is equal to 0\n");
		}	else if
			(n < 0);
		{
			printf("%d is negative\n")
		}
	return (0);
}
