#include "main.h"
/**
 * _abs - computing the value of an integer
 * @n: where the value is stored
 *
 * Return: the integer value
 */

int _abs(int n)
{
	if (n > 0)

		return (n);
	else
		return (-n);

}
